# MaxHire-Java

A Java re-implementation of the MaxHire backend built with Java, Spring, and PostgreSQL.

This project recreates the exact behavior and API endpoints from the [original project repository](https://github.com/mae1579/MaxHire-Backend), letting the existing frontend run with zero client-side changes.
